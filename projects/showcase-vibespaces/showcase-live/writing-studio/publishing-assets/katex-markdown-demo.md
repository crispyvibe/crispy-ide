# KaTeX in Markdown

Inline math like $E = mc^2$ and $a^2 + b^2 = c^2$ should render in the preview.

Display math:

$$
\int_{-\infty}^{\infty} e^{-x^2}\,dx = \sqrt{\pi}
$$

A list with math:

- Euler: $e^{i\pi} + 1 = 0$
- Sum: $\sum_{k=1}^{n} k = \frac{n(n+1)}{2}$

And a normal code block (should NOT render as math):

```
let price = $5 + $3
```
